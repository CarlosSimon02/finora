import { Account } from "@/presentation/components/Features/Account";

export const metadata = {
  title: "Account",
  description: "Manage your account settings and preferences",
};

const AccountPage = () => {
  return <Account />;
};

export default AccountPage;
